# The Fibenchi endpoint the app pulls its config bundle from (no trailing slash).
# Copy this file to .env.local and set your own value — .env.local is gitignored,
# so your host never lands in source control.
EXPO_PUBLIC_FIBENCHI_ENDPOINT=
